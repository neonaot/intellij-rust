name: Notify users based on labels
on:
  issues:
      types: [labeled]
  pull_request:
      types: [labeled]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
        - uses: bugobliterator/issue-label-notification-action@pr-pr-notify
          with:
             recipients: |
                  help wanted=@neonaot
                  documentation=@neonaot
             message: '{receipients} look here, there is label {label}!'
